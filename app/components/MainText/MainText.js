import { Paper } from "@mui/material"

const MainText = () => {

    return (
        <Paper elevation={3} sx={{p:1}}>
        Choose or Search municipal transport. Please, choose tranport Type first, if you use search. After that, the input field will become available and you can use it.
        </Paper>
    )

}

export default MainText